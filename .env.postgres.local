# Local PostgreSQL Development Environment
# Use this file to test PostgreSQL locally before production deployment

# PostgreSQL connection (Docker)
DATABASE_URL="postgresql://messai:messai_dev_password@localhost:5432/messai_dev?schema=public"

# Copy other settings from .env.local
NODE_ENV="development"